# Base URL of the K21 Node API. All client requests target `${NEXT_PUBLIC_API_URL}/api/...`.
# This is exposed to the browser (NEXT_PUBLIC_*), so it must be a public-reachable URL.
NEXT_PUBLIC_API_URL=http://localhost:4000
