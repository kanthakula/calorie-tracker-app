# pnpm settings for this monorepo.
# Hoist nothing by default (strict), but allow common build tools to be found.
auto-install-peers=true
strict-peer-dependencies=false
# Expo / React Native and Next.js work best with a shamefully-hoisted layout.
shamefully-hoist=true
